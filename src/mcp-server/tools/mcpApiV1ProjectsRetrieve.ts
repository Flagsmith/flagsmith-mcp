/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { mcpApiV1ProjectsRetrieve } from "../../funcs/mcpApiV1ProjectsRetrieve.js";
import { ApiV1ProjectsRetrieveRequest$zodSchema } from "../../models/apiv1projectsretrieveop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ApiV1ProjectsRetrieveRequest$zodSchema,
};

export const tool$mcpApiV1ProjectsRetrieve: ToolDefinition<typeof args> = {
  name: "mcp-api-v1-projects-retrieve",
  description: ``,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await mcpApiV1ProjectsRetrieve(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
