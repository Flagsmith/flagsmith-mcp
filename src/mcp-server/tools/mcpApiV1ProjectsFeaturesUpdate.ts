/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { mcpApiV1ProjectsFeaturesUpdate } from "../../funcs/mcpApiV1ProjectsFeaturesUpdate.js";
import { ApiV1ProjectsFeaturesUpdateRequest$zodSchema } from "../../models/apiv1projectsfeaturesupdateop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ApiV1ProjectsFeaturesUpdateRequest$zodSchema,
};

export const tool$mcpApiV1ProjectsFeaturesUpdate: ToolDefinition<typeof args> =
  {
    name: "mcp-api-v1-projects-features-update",
    description: ``,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await mcpApiV1ProjectsFeaturesUpdate(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
