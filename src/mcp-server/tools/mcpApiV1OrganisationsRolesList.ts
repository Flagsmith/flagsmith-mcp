/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { mcpApiV1OrganisationsRolesList } from "../../funcs/mcpApiV1OrganisationsRolesList.js";
import { ApiV1OrganisationsRolesListRequest$zodSchema } from "../../models/apiv1organisationsroleslistop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ApiV1OrganisationsRolesListRequest$zodSchema,
};

export const tool$mcpApiV1OrganisationsRolesList: ToolDefinition<typeof args> =
  {
    name: "mcp-api-v1-organisations-roles-list",
    description: ``,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": true,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await mcpApiV1OrganisationsRolesList(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
