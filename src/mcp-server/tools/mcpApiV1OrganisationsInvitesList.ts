/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { mcpApiV1OrganisationsInvitesList } from "../../funcs/mcpApiV1OrganisationsInvitesList.js";
import { ApiV1OrganisationsInvitesListRequest$zodSchema } from "../../models/apiv1organisationsinviteslistop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ApiV1OrganisationsInvitesListRequest$zodSchema,
};

export const tool$mcpApiV1OrganisationsInvitesList: ToolDefinition<
  typeof args
> = {
  name: "mcp-api-v1-organisations-invites-list",
  description: ``,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await mcpApiV1OrganisationsInvitesList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
