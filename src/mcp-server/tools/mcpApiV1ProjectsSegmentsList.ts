/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { mcpApiV1ProjectsSegmentsList } from "../../funcs/mcpApiV1ProjectsSegmentsList.js";
import { ApiV1ProjectsSegmentsListRequest$zodSchema } from "../../models/apiv1projectssegmentslistop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ApiV1ProjectsSegmentsListRequest$zodSchema,
};

export const tool$mcpApiV1ProjectsSegmentsList: ToolDefinition<typeof args> = {
  name: "mcp-api-v1-projects-segments-list",
  description: ``,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await mcpApiV1ProjectsSegmentsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
