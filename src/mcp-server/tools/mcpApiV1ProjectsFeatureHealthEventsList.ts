/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { mcpApiV1ProjectsFeatureHealthEventsList } from "../../funcs/mcpApiV1ProjectsFeatureHealthEventsList.js";
import { ApiV1ProjectsFeatureHealthEventsListRequest$zodSchema } from "../../models/apiv1projectsfeaturehealtheventslistop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ApiV1ProjectsFeatureHealthEventsListRequest$zodSchema,
};

export const tool$mcpApiV1ProjectsFeatureHealthEventsList: ToolDefinition<
  typeof args
> = {
  name: "mcp-api-v1-projects-feature-health-events-list",
  description: ``,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await mcpApiV1ProjectsFeatureHealthEventsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
